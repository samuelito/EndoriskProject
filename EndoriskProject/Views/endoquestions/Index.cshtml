@model IEnumerable<EndoriskProject.Models.endoquestion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.endoQuestion1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.abbr)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.choiceSet)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.endoQuestion1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.abbr)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.choiceSet)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idQuestion }) |
            @Html.ActionLink("Details", "Details", new { id=item.idQuestion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idQuestion })
        </td>
    </tr>
}

</table>
