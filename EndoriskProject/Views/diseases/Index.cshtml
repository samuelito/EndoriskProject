@model IEnumerable<EndoriskProject.Models.disease>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.disease1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idRomeQuestion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.criteria)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.comparedValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.disease1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idRomeQuestion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.criteria)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.comparedValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idDiseases }) |
            @Html.ActionLink("Details", "Details", new { id=item.idDiseases }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idDiseases })
        </td>
    </tr>
}

</table>
