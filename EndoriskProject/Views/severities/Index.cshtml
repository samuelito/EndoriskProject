@model IEnumerable<EndoriskProject.Models.severity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idQuiz)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.severity1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idQuiz)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.severity1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idSeverity }) |
            @Html.ActionLink("Details", "Details", new { id=item.idSeverity }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idSeverity })
        </td>
    </tr>
}

</table>
